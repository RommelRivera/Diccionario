<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/diccionario"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/diccionario_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".ColorDialog"
            android:exported="false"
            android:theme="@style/Theme.Material3.DayNight.Dialog"
            android:excludeFromRecents="true" />
        <activity
            android:name=".CrearEditarIdioma"
            android:exported="false" />
        <activity
            android:name=".CrearEditarPalabra"
            android:exported="false" />
        <activity
            android:name=".Definicion"
            android:exported="false" />
        <activity
            android:name=".Lista"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>